@page "/tich-hop-his"
@using System.Globalization

@{
    var lang = CultureInfo.CurrentCulture.TwoLetterISOLanguageName.ToLowerInvariant();
}

@if (lang == "en")
{
    <PageTitle>HIS / EMR Integration — BiliTool.Vn API & Deep Link</PageTitle>
    <HeadContent>
        <meta name="description" content="Technical integration guide for connecting BiliTool.Vn to HIS/EMR systems: Deep Link, REST API, API key, input parameters, enums, response fields, and error handling." />
        <meta name="keywords" content="BiliTool.Vn HIS EMR API, electronic medical record integration, bilirubin deep link, neonatal bilirubin API" />
        <link rel="canonical" href="https://bilitool.vn/tich-hop-his" />
        <meta property="og:type" content="article" />
        <meta property="og:title" content="Integrate BiliTool.Vn with HIS / EMR" />
        <meta property="og:description" content="Deep Link and REST API integration for neonatal bilirubin threshold calculation in hospital software." />
        <meta property="og:url" content="https://bilitool.vn/tich-hop-his" />
        <meta property="og:site_name" content="BiliTool.Vn" />
        <meta property="og:image" content="https://bilitool.vn/images/og-bilitool-1200x630.png" />
        <meta name="twitter:card" content="summary_large_image" />
    </HeadContent>

    <div class="his-doc-shell">
        <section class="his-hero-panel">
            <div class="his-kicker"><i class="fas fa-hospital-alt"></i> HIS / EMR Integration</div>
            <h1>HIS / EMR system integration</h1>
            <p>BiliTool.Vn supports two integration modes: open the calculator UI through a Deep Link for clinician review, or call a secure REST API with <code>X-API-Key</code> so the HIS receives structured JSON results.</p>
            <div class="his-status-grid" aria-label="Documentation review status">
                <div><strong>Verified</strong><span>Route, DTO, validator, response</span></div>
                <div><strong>Secured API</strong><span><code>X-API-Key</code> + rate limit</span></div>
                <div><strong>Scope</strong><span>Infants ≥35 weeks, age 1–336 hours</span></div>
            </div>
        </section>
        <section class="his-audit-card">
            <h2><i class="fas fa-clipboard-check"></i> Quick review conclusion</h2>
            <ul class="his-check-list">
                <li><i class="fas fa-check"></i><span><strong>Correct:</strong> Deep Link uses route <code>/may-tinh</code> and query parameters <code>ngaysinh</code>, <code>giosinh</code>, <code>ngaymau</code>, <code>giomau</code>, <code>bili</code>, <code>donvi</code>, <code>tuoithai</code>, <code>autocalc</code>.</span></li>
                <li><i class="fas fa-check"></i><span><strong>Correct:</strong> The official endpoint is <code>POST /api/v1/bilirubin/calculate</code>, accepts <code>YeuCauTinhToanBilirubinDto</code>, and returns <code>KetQuaTinhToanDto</code>.</span></li>
                <li><i class="fas fa-triangle-exclamation"></i><span><strong>Clarify:</strong> Deep Link does not currently pass neurotoxicity or clinical risk factors. Use the REST API when the HIS must send full risk data.</span></li>
                <li><i class="fas fa-triangle-exclamation"></i><span><strong>Clarify:</strong> JSON enum values should be numeric: <code>donViDo: 0</code> = mg/dL and <code>donViDo: 1</code> = µmol/L. Do not send <code>"MgDl"</code> in JSON API requests.</span></li>
                <li><i class="fas fa-triangle-exclamation"></i><span><strong>Clarify:</strong> API validation currently limits age to <code>1–336 hours</code>. Wider ranges in older documents may be misleading for integration.</span></li>
            </ul>
        </section>
        <section class="his-method-card"><div class="his-method-heading"><span>Method 1</span><h2>Deep Link to open the calculator UI</h2></div>
            <p>Best when the HIS opens BiliTool.Vn in a new tab or window, pre-fills basic data, and lets the clinician review before calculating. Deep Link does not require an API key because it does not call the JSON endpoint.</p>
            <div class="his-endpoint" tabindex="0"><code>GET</code><span>https://bilitool.vn/may-tinh?ngaysinh=15/03/2026&amp;giosinh=08:30&amp;ngaymau=16/03/2026&amp;giomau=10:00&amp;bili=12.5&amp;donvi=MgDl&amp;tuoithai=38&amp;autocalc=true</span></div>
            <div class="table-responsive" tabindex="0"><table class="his-table"><thead><tr><th>Parameter</th><th>Required</th><th>Valid format</th><th>Notes</th></tr></thead><tbody>
                <tr><td><code>ngaysinh</code></td><td>Yes for auto calculation</td><td><code>dd/MM/yyyy</code>, <code>d/M/yyyy</code>, or <code>ddMMyyyy</code></td><td>Birth date.</td></tr>
                <tr><td><code>giosinh</code></td><td>Recommended</td><td><code>HH:mm</code> or <code>HHmm</code></td><td>If omitted, the UI uses its default time.</td></tr>
                <tr><td><code>ngaymau</code></td><td>Yes for auto calculation</td><td><code>dd/MM/yyyy</code>, <code>d/M/yyyy</code>, or <code>ddMMyyyy</code></td><td>Bilirubin sample date.</td></tr>
                <tr><td><code>giomau</code></td><td>Recommended</td><td><code>HH:mm</code> or <code>HHmm</code></td><td>If omitted, the UI uses its default time.</td></tr>
                <tr><td><code>bili</code></td><td>Yes for auto calculation</td><td>Decimal number with dot, for example <code>12.5</code></td><td>Total bilirubin.</td></tr>
                <tr><td><code>donvi</code></td><td>No</td><td><code>MgDl</code> or <code>UmolL</code></td><td>The UI default is <code>UmolL</code>.</td></tr>
                <tr><td><code>tuoithai</code></td><td>Yes for auto calculation</td><td>Integer <code>35–45</code></td><td>Gestational age in completed weeks.</td></tr>
                <tr><td><code>autocalc</code></td><td>No</td><td><code>true</code> or <code>false</code></td><td><code>true</code> runs calculation after page load.</td></tr>
            </tbody></table></div>
            <p class="his-note"><i class="fas fa-info-circle"></i> Deep Link currently does not pass risk factors, ETCOc, or phototherapy status. Clinicians must select those in the UI, or the HIS must call the REST API.</p>
            <h3>JavaScript example inside an EMR</h3>
            <pre class="his-code"><code>// Open BiliTool.Vn from an EMR using Deep Link
const url = new URL("https://bilitool.vn/may-tinh");
url.searchParams.append("ngaysinh", "15/03/2026");
url.searchParams.append("giosinh", "08:30");
url.searchParams.append("ngaymau", "16/03/2026");
url.searchParams.append("giomau", "10:00");
url.searchParams.append("bili", "12.5");
url.searchParams.append("donvi", "MgDl");
url.searchParams.append("tuoithai", "38");
url.searchParams.append("autocalc", "true");
window.open(url.toString(), "_blank", "width=1200,height=800");</code></pre>
        </section>
        <section class="his-method-card"><div class="his-method-heading"><span>Method 2</span><h2>REST API for headless calculation</h2></div>
            <p>Use this mode when the HIS needs JSON results for storage in the electronic record, display inside an internal screen, or automated workflow. The API requires <code>X-API-Key</code> and currently applies a 30 requests per minute rate limit window.</p>
            <p class="his-note"><i class="fas fa-lock"></i> Production API keys are issued only for integration partners. If no key is configured the API returns 503; missing or invalid keys return 401.</p>
            <div class="his-endpoint" tabindex="0"><code>POST</code><span>https://bilitool.vn/api/v1/bilirubin/calculate</span></div>
            <h3>Required header</h3><pre class="his-code"><code>X-API-Key: {your_api_key}
Content-Type: application/json</code></pre>
            <h3>Main input fields</h3>
            <div class="table-responsive" tabindex="0"><table class="his-table"><thead><tr><th>JSON field</th><th>Required</th><th>Type</th><th>Limits / meaning</th></tr></thead><tbody>
                <tr><td><code>tuoiTheoGio</code></td><td>Yes if date/time omitted</td><td>number</td><td>Age at sampling, valid <code>1–336</code> hours.</td></tr>
                <tr><td><code>ngaySinh</code></td><td>Yes if age omitted</td><td>ISO datetime</td><td>May include time, for example <code>2026-03-15T08:30:00+07:00</code>.</td></tr>
                <tr><td><code>gioSinh</code></td><td>No</td><td><code>HH:mm:ss</code></td><td>If sent separately, system combines it with <code>ngaySinh</code>.</td></tr>
                <tr><td><code>ngayLayMau</code></td><td>Yes if age omitted</td><td>ISO datetime</td><td>Must be after birth; computed age must be <code>1–336</code> hours.</td></tr>
                <tr><td><code>gioLayMau</code></td><td>No</td><td><code>HH:mm:ss</code></td><td>If sent separately, system combines it with <code>ngayLayMau</code>.</td></tr>
                <tr><td><code>tongBilirubin</code></td><td>Yes</td><td>number</td><td>Greater than 0; maximum 60 for mg/dL, 1026 for µmol/L.</td></tr>
                <tr><td><code>donViDo</code></td><td>No</td><td>number enum</td><td><code>0 = MgDl</code>, <code>1 = UmolL</code>. Default is 1.</td></tr>
                <tr><td><code>tuoiThaiTuan</code></td><td>Yes</td><td>integer</td><td>Valid <code>35–45</code> completed weeks.</td></tr>
                <tr><td><code>yeuToNguyCo</code></td><td>No</td><td>object</td><td>If omitted, all risk factors default to false or null.</td></tr>
                <tr><td><code>trangThaiChieuDen</code></td><td>No</td><td>number enum</td><td>0 no phototherapy, 1 ongoing, 2 intensive, 3 stopped.</td></tr>
            </tbody></table></div>
            <h3>Supported risk factor groups</h3>
            <div class="his-risk-grid"><div><strong>AAP 2022 - neurotoxicity risk</strong><span>Isoimmune hemolysis or G6PD deficiency, sepsis or suspected sepsis, albumin &lt;3 g/dL, clinical instability, ETCOc if available.</span></div><div><strong>NICE CG98 - clinical and extended risk</strong><span>Sibling previously treated with phototherapy, exclusive breastfeeding intent, jaundice in first 24 hours, Rh/ABO hemolysis, acute bilirubin encephalopathy signs.</span></div></div>
            <h3>Request example matching the current DTO</h3>
            <pre class="his-code"><code>{
  "ngaySinh": "2026-03-15T08:30:00+07:00",
  "ngayLayMau": "2026-03-16T10:00:00+07:00",
  "tongBilirubin": 12.5,
  "donViDo": 0,
  "tuoiThaiTuan": 38,
  "yeuToNguyCo": {
    "benhTanHuyetMienDichHoacThieuG6PD": false,
    "nhiemKhuanHuyetHoacNghiNgo": false,
    "albuminThapDuoi3gDl": false,
    "tinhTrangLamSangKhongOnDinh": false,
    "etCOcPpm": null,
    "anhChiBiVangDaCanChieuDen": false,
    "meBuMeHoanToan": false,
    "vangDaTrong24hDau": false,
    "tanHuyetRh": false,
    "tanHuyetABO": false,
    "dauHieuBenhNaoBilirubinCap": false
  },
  "trangThaiChieuDen": 0
}</code></pre>
            <h3>Important response fields</h3>
            <div class="table-responsive" tabindex="0"><table class="his-table"><thead><tr><th>Response field</th><th>Meaning</th></tr></thead><tbody>
                <tr><td><code>tuoiGio</code>, <code>tuoiThaiTuan</code></td><td>Patient context after system calculation and normalization.</td></tr>
                <tr><td><code>bilirubinMgDl</code>, <code>bilirubinUmolL</code></td><td>Bilirubin value converted in both units.</td></tr>
                <tr><td><code>nguongChieuDen</code>, <code>nguongChieuDenTichCuc</code>, <code>nguongThayCuuMau</code></td><td>AAP 2022 thresholds in mg/dL.</td></tr>
                <tr><td><code>nguongChieuDen_NICE_UmolL</code>, <code>nguongThayCuuMau_NICE_UmolL</code></td><td>NICE CG98 thresholds in µmol/L.</td></tr>
                <tr><td><code>canChieuDenNgay</code>, <code>canChieuDenTichCuc</code>, <code>canXemXetThayCuuMau</code></td><td>Decision flags for HIS alerts.</td></tr>
                <tr><td><code>gioDoLapTiepTheo</code>, <code>lichDoLapNICE</code></td><td>Suggested repeat-measurement timing when returned by the guideline engine.</td></tr>
                <tr><td><code>phacDoQuyetDinh</code>, <code>chuThichThamChieu</code></td><td>Deciding guideline and source note.</td></tr>
                <tr><td><code>chartData</code></td><td>Nomogram data for HIS-side chart rendering if needed.</td></tr>
            </tbody></table></div>
        </section>
        <section class="his-method-card"><div class="his-method-heading"><span>Advanced reference</span><h2>Clinical wrapper endpoint</h2></div><p>Endpoint <code>POST /api/v2/clinical/bilirubin/calculate</code> uses the same input as API v1, but wraps the result as <code>guideline</code>, <code>patientContext</code>, <code>thresholds</code>, <code>recommendation</code>, and <code>legacyResult</code>. This endpoint also requires <code>X-API-Key</code>.</p></section>
        <section class="his-method-card"><div class="his-method-heading"><span>Operations</span><h2>Error codes the HIS should handle</h2></div><div class="his-error-grid"><div><strong>400</strong><span>Invalid data: missing age or sample date, age outside 1–336 hours, gestation outside 35–45 weeks, or bilirubin above limit.</span></div><div><strong>401</strong><span>Missing or invalid <code>X-API-Key</code>.</span></div><div><strong>429</strong><span>API rate limit exceeded.</span></div><div><strong>503</strong><span>Server integration API key list is not configured.</span></div><div><strong>500</strong><span>System error; response includes <code>traceId</code> for log correlation.</span></div></div></section>
    </div>
}
else if (lang == "fr")
{
    <PageTitle>Intégration HIS / EMR — API et lien profond BiliTool.Vn</PageTitle>
    <HeadContent>
        <meta name="description" content="Guide technique pour intégrer BiliTool.Vn aux systèmes HIS/EMR : lien profond, API REST, clé API, paramètres d’entrée, énumérations, réponse et gestion des erreurs." />
        <meta name="keywords" content="BiliTool.Vn HIS EMR API, intégration dossier médical électronique, lien profond bilirubine, API bilirubine néonatale" />
        <link rel="canonical" href="https://bilitool.vn/tich-hop-his" />
        <meta property="og:type" content="article" />
        <meta property="og:title" content="Intégrer BiliTool.Vn au HIS / EMR" />
        <meta property="og:description" content="Lien profond et API REST pour intégrer le calcul des seuils de bilirubine néonatale dans les logiciels hospitaliers." />
        <meta property="og:url" content="https://bilitool.vn/tich-hop-his" />
        <meta property="og:site_name" content="BiliTool.Vn" />
        <meta property="og:image" content="https://bilitool.vn/images/og-bilitool-1200x630.png" />
        <meta name="twitter:card" content="summary_large_image" />
    </HeadContent>

    <div class="his-doc-shell">
        <section class="his-hero-panel"><div class="his-kicker"><i class="fas fa-hospital-alt"></i> Intégration HIS / EMR</div><h1>Intégration aux systèmes HIS / EMR</h1><p>BiliTool.Vn prend en charge deux modes : ouvrir l’interface du calculateur par lien profond pour revue médicale, ou appeler une API REST sécurisée avec <code>X-API-Key</code> afin que le HIS reçoive des résultats JSON structurés.</p><div class="his-status-grid" aria-label="État de revue de la documentation"><div><strong>Vérifié</strong><span>Route, DTO, validateur, réponse</span></div><div><strong>API sécurisée</strong><span><code>X-API-Key</code> + limite de débit</span></div><div><strong>Champ</strong><span>Nouveau-nés ≥35 semaines, âge 1–336 heures</span></div></div></section>
        <section class="his-audit-card"><h2><i class="fas fa-clipboard-check"></i> Conclusion de revue rapide</h2><ul class="his-check-list"><li><i class="fas fa-check"></i><span><strong>Correct :</strong> le lien profond utilise la route <code>/may-tinh</code> et les paramètres <code>ngaysinh</code>, <code>giosinh</code>, <code>ngaymau</code>, <code>giomau</code>, <code>bili</code>, <code>donvi</code>, <code>tuoithai</code>, <code>autocalc</code>.</span></li><li><i class="fas fa-check"></i><span><strong>Correct :</strong> l’endpoint officiel est <code>POST /api/v1/bilirubin/calculate</code>, accepte <code>YeuCauTinhToanBilirubinDto</code> et retourne <code>KetQuaTinhToanDto</code>.</span></li><li><i class="fas fa-triangle-exclamation"></i><span><strong>À clarifier :</strong> le lien profond ne transmet pas encore les facteurs de risque neurologiques ou cliniques. Utiliser l’API REST si le HIS doit envoyer toutes les données de risque.</span></li><li><i class="fas fa-triangle-exclamation"></i><span><strong>À clarifier :</strong> les énumérations JSON doivent être numériques : <code>donViDo: 0</code> = mg/dL et <code>donViDo: 1</code> = µmol/L. Ne pas envoyer <code>"MgDl"</code> dans les requêtes JSON.</span></li><li><i class="fas fa-triangle-exclamation"></i><span><strong>À clarifier :</strong> la validation API limite actuellement l’âge à <code>1–336 heures</code>. Les plages plus larges des anciens documents peuvent induire en erreur.</span></li></ul></section>
        <section class="his-method-card"><div class="his-method-heading"><span>Méthode 1</span><h2>Lien profond vers l’interface du calculateur</h2></div><p>Approprié lorsque le HIS ouvre BiliTool.Vn dans un nouvel onglet ou une nouvelle fenêtre, préremplit les données de base et laisse le clinicien vérifier avant le calcul. Le lien profond ne nécessite pas de clé API car il n’appelle pas l’endpoint JSON.</p><div class="his-endpoint" tabindex="0"><code>GET</code><span>https://bilitool.vn/may-tinh?ngaysinh=15/03/2026&amp;giosinh=08:30&amp;ngaymau=16/03/2026&amp;giomau=10:00&amp;bili=12.5&amp;donvi=MgDl&amp;tuoithai=38&amp;autocalc=true</span></div><div class="table-responsive" tabindex="0"><table class="his-table"><thead><tr><th>Paramètre</th><th>Obligatoire</th><th>Format valide</th><th>Notes</th></tr></thead><tbody><tr><td><code>ngaysinh</code></td><td>Oui pour calcul automatique</td><td><code>dd/MM/yyyy</code>, <code>d/M/yyyy</code> ou <code>ddMMyyyy</code></td><td>Date de naissance.</td></tr><tr><td><code>giosinh</code></td><td>Recommandé</td><td><code>HH:mm</code> ou <code>HHmm</code></td><td>Si absent, l’interface utilise l’heure par défaut.</td></tr><tr><td><code>ngaymau</code></td><td>Oui pour calcul automatique</td><td><code>dd/MM/yyyy</code>, <code>d/M/yyyy</code> ou <code>ddMMyyyy</code></td><td>Date du prélèvement de bilirubine.</td></tr><tr><td><code>giomau</code></td><td>Recommandé</td><td><code>HH:mm</code> ou <code>HHmm</code></td><td>Si absent, l’interface utilise l’heure par défaut.</td></tr><tr><td><code>bili</code></td><td>Oui pour calcul automatique</td><td>Nombre décimal avec point, par exemple <code>12.5</code></td><td>Bilirubine totale.</td></tr><tr><td><code>donvi</code></td><td>Non</td><td><code>MgDl</code> ou <code>UmolL</code></td><td>Unité par défaut de l’interface : <code>UmolL</code>.</td></tr><tr><td><code>tuoithai</code></td><td>Oui pour calcul automatique</td><td>Entier <code>35–45</code></td><td>Âge gestationnel en semaines révolues.</td></tr><tr><td><code>autocalc</code></td><td>Non</td><td><code>true</code> ou <code>false</code></td><td><code>true</code> lance le calcul au chargement.</td></tr></tbody></table></div><p class="his-note"><i class="fas fa-info-circle"></i> Le lien profond ne transmet pas encore les facteurs de risque, l’ETCOc ou l’état de photothérapie. Le clinicien doit les sélectionner dans l’interface, ou le HIS doit appeler l’API REST.</p><h3>Exemple JavaScript dans un EMR</h3><pre class="his-code"><code>// Ouvrir BiliTool.Vn depuis un EMR avec un lien profond
const url = new URL("https://bilitool.vn/may-tinh");
url.searchParams.append("ngaysinh", "15/03/2026");
url.searchParams.append("giosinh", "08:30");
url.searchParams.append("ngaymau", "16/03/2026");
url.searchParams.append("giomau", "10:00");
url.searchParams.append("bili", "12.5");
url.searchParams.append("donvi", "MgDl");
url.searchParams.append("tuoithai", "38");
url.searchParams.append("autocalc", "true");
window.open(url.toString(), "_blank", "width=1200,height=800");</code></pre></section>
        <section class="his-method-card"><div class="his-method-heading"><span>Méthode 2</span><h2>API REST pour calcul sans interface</h2></div><p>Utiliser ce mode lorsque le HIS doit recevoir des résultats JSON pour les enregistrer dans le dossier électronique, les afficher dans un écran interne ou intégrer un flux automatisé. L’API exige <code>X-API-Key</code> et applique actuellement une limite de 30 requêtes par minute.</p><p class="his-note"><i class="fas fa-lock"></i> Les clés API de production sont attribuées uniquement aux partenaires d’intégration. Si aucune clé n’est configurée, l’API retourne 503; une clé absente ou invalide retourne 401.</p><div class="his-endpoint" tabindex="0"><code>POST</code><span>https://bilitool.vn/api/v1/bilirubin/calculate</span></div><h3>En-tête obligatoire</h3><pre class="his-code"><code>X-API-Key: {your_api_key}
Content-Type: application/json</code></pre><h3>Champs d’entrée principaux</h3><div class="table-responsive" tabindex="0"><table class="his-table"><thead><tr><th>Champ JSON</th><th>Obligatoire</th><th>Type</th><th>Limites / signification</th></tr></thead><tbody><tr><td><code>tuoiTheoGio</code></td><td>Oui si date/heure absentes</td><td>number</td><td>Âge au prélèvement, valide <code>1–336</code> heures.</td></tr><tr><td><code>ngaySinh</code></td><td>Oui si âge absent</td><td>Date ISO</td><td>Peut inclure l’heure, par exemple <code>2026-03-15T08:30:00+07:00</code>.</td></tr><tr><td><code>gioSinh</code></td><td>Non</td><td><code>HH:mm:ss</code></td><td>Si envoyé séparément, le système le combine avec <code>ngaySinh</code>.</td></tr><tr><td><code>ngayLayMau</code></td><td>Oui si âge absent</td><td>Date ISO</td><td>Doit être après la naissance; l’âge calculé doit être <code>1–336</code> heures.</td></tr><tr><td><code>gioLayMau</code></td><td>Non</td><td><code>HH:mm:ss</code></td><td>Si envoyé séparément, le système le combine avec <code>ngayLayMau</code>.</td></tr><tr><td><code>tongBilirubin</code></td><td>Oui</td><td>number</td><td>Supérieur à 0; maximum 60 pour mg/dL, 1026 pour µmol/L.</td></tr><tr><td><code>donViDo</code></td><td>Non</td><td>enum numérique</td><td><code>0 = MgDl</code>, <code>1 = UmolL</code>. Valeur par défaut : 1.</td></tr><tr><td><code>tuoiThaiTuan</code></td><td>Oui</td><td>integer</td><td>Valide <code>35–45</code> semaines révolues.</td></tr><tr><td><code>yeuToNguyCo</code></td><td>Non</td><td>object</td><td>Si absent, tous les facteurs de risque valent false ou null.</td></tr><tr><td><code>trangThaiChieuDen</code></td><td>Non</td><td>enum numérique</td><td>0 sans photothérapie, 1 en cours, 2 intensive, 3 arrêtée.</td></tr></tbody></table></div><h3>Groupes de facteurs de risque pris en charge</h3><div class="his-risk-grid"><div><strong>AAP 2022 - risque de neurotoxicité</strong><span>Hémolyse iso-immune ou déficit G6PD, sepsis ou suspicion, albumine &lt;3 g/dL, instabilité clinique, ETCOc si disponible.</span></div><div><strong>NICE CG98 - risque clinique et étendu</strong><span>Fratrie ayant reçu une photothérapie, allaitement maternel exclusif prévu, ictère dans les 24 premières heures, hémolyse Rh/ABO, signes d’encéphalopathie bilirubinique aiguë.</span></div></div><h3>Exemple de requête conforme au DTO actuel</h3><pre class="his-code"><code>{
  "ngaySinh": "2026-03-15T08:30:00+07:00",
  "ngayLayMau": "2026-03-16T10:00:00+07:00",
  "tongBilirubin": 12.5,
  "donViDo": 0,
  "tuoiThaiTuan": 38,
  "yeuToNguyCo": {
    "benhTanHuyetMienDichHoacThieuG6PD": false,
    "nhiemKhuanHuyetHoacNghiNgo": false,
    "albuminThapDuoi3gDl": false,
    "tinhTrangLamSangKhongOnDinh": false,
    "etCOcPpm": null,
    "anhChiBiVangDaCanChieuDen": false,
    "meBuMeHoanToan": false,
    "vangDaTrong24hDau": false,
    "tanHuyetRh": false,
    "tanHuyetABO": false,
    "dauHieuBenhNaoBilirubinCap": false
  },
  "trangThaiChieuDen": 0
}</code></pre><h3>Champs de réponse importants</h3><div class="table-responsive" tabindex="0"><table class="his-table"><thead><tr><th>Champ de réponse</th><th>Signification</th></tr></thead><tbody><tr><td><code>tuoiGio</code>, <code>tuoiThaiTuan</code></td><td>Contexte patient après calcul et normalisation.</td></tr><tr><td><code>bilirubinMgDl</code>, <code>bilirubinUmolL</code></td><td>Bilirubine convertie dans les deux unités.</td></tr><tr><td><code>nguongChieuDen</code>, <code>nguongChieuDenTichCuc</code>, <code>nguongThayCuuMau</code></td><td>Seuils AAP 2022 en mg/dL.</td></tr><tr><td><code>nguongChieuDen_NICE_UmolL</code>, <code>nguongThayCuuMau_NICE_UmolL</code></td><td>Seuils NICE CG98 en µmol/L.</td></tr><tr><td><code>canChieuDenNgay</code>, <code>canChieuDenTichCuc</code>, <code>canXemXetThayCuuMau</code></td><td>Indicateurs décisionnels pour alertes HIS.</td></tr><tr><td><code>gioDoLapTiepTheo</code>, <code>lichDoLapNICE</code></td><td>Suggestion de contrôle répété si retournée par le moteur.</td></tr><tr><td><code>phacDoQuyetDinh</code>, <code>chuThichThamChieu</code></td><td>Recommandation déterminante et note de source.</td></tr><tr><td><code>chartData</code></td><td>Données nomogramme pour un graphique côté HIS si nécessaire.</td></tr></tbody></table></div></section>
        <section class="his-method-card"><div class="his-method-heading"><span>Référence avancée</span><h2>Endpoint avec enveloppe clinique</h2></div><p>L’endpoint <code>POST /api/v2/clinical/bilirubin/calculate</code> utilise la même entrée que l’API v1, mais enveloppe le résultat dans <code>guideline</code>, <code>patientContext</code>, <code>thresholds</code>, <code>recommendation</code> et <code>legacyResult</code>. Il exige aussi <code>X-API-Key</code>.</p></section>
        <section class="his-method-card"><div class="his-method-heading"><span>Exploitation</span><h2>Codes d’erreur à gérer côté HIS</h2></div><div class="his-error-grid"><div><strong>400</strong><span>Données invalides : âge ou date de prélèvement manquants, âge hors 1–336 heures, gestation hors 35–45 semaines ou bilirubine au-dessus de la limite.</span></div><div><strong>401</strong><span><code>X-API-Key</code> absent ou invalide.</span></div><div><strong>429</strong><span>Limite de débit API dépassée.</span></div><div><strong>503</strong><span>Liste des clés API d’intégration non configurée sur le serveur.</span></div><div><strong>500</strong><span>Erreur système; la réponse inclut <code>traceId</code> pour corrélation des journaux.</span></div></div></section>
    </div>
}
else
{
<div class="his-doc-shell">
    <section class="his-hero-panel">
        <div class="his-kicker"><i class="fas fa-hospital-alt"></i> HIS / EMR Integration</div>
        <h1>Tích hợp hệ thống HIS / EMR</h1>
        <p>BiliTool.Vn hỗ trợ 2 cách tích hợp: mở giao diện bằng Deep Link để bác sĩ kiểm tra trực tiếp, hoặc gọi REST API bảo mật bằng <code>X-API-Key</code> để HIS nhận kết quả JSON.</p>
        <div class="his-status-grid" aria-label="Trạng thái rà soát tài liệu">
            <div><strong>Đã đối chiếu</strong><span>Route, DTO, validator, response</span></div>
            <div><strong>API bảo mật</strong><span><code>X-API-Key</code> + rate limit</span></div>
            <div><strong>Phạm vi</strong><span>Trẻ ≥35 tuần, tuổi 1–336 giờ</span></div>
        </div>
    </section>

    <section class="his-audit-card">
        <h2><i class="fas fa-clipboard-check"></i> Kết luận rà soát nhanh</h2>
        <ul class="his-check-list">
            <li><i class="fas fa-check"></i><span><strong>Đúng:</strong> Deep Link hiện có route <code>/may-tinh</code> và các query <code>ngaysinh</code>, <code>giosinh</code>, <code>ngaymau</code>, <code>giomau</code>, <code>bili</code>, <code>donvi</code>, <code>tuoithai</code>, <code>autocalc</code>.</span></li>
            <li><i class="fas fa-check"></i><span><strong>Đúng:</strong> API chính thức là <code>POST /api/v1/bilirubin/calculate</code>, nhận <code>YeuCauTinhToanBilirubinDto</code>, trả <code>KetQuaTinhToanDto</code>.</span></li>
            <li><i class="fas fa-triangle-exclamation"></i><span><strong>Cần làm rõ:</strong> Deep Link chưa truyền được yếu tố nguy cơ thần kinh/lâm sàng; nếu HIS cần truyền đầy đủ nguy cơ, phải dùng REST API.</span></li>
            <li><i class="fas fa-triangle-exclamation"></i><span><strong>Cần làm rõ:</strong> JSON enum hiện nên gửi dạng số: <code>donViDo: 0</code> = mg/dL, <code>donViDo: 1</code> = µmol/L; không ghi <code>"MgDl"</code> trong JSON API.</span></li>
            <li><i class="fas fa-triangle-exclamation"></i><span><strong>Cần làm rõ:</strong> API validation đang giới hạn tuổi <code>1–336 giờ</code>; tài liệu cũ ghi phạm vi rộng hơn gây hiểu nhầm khi tích hợp.</span></li>
        </ul>
    </section>

    <section class="his-method-card">
        <div class="his-method-heading">
            <span>Phương pháp 1</span>
            <h2>Deep Link mở giao diện máy tính</h2>
        </div>
        <p>Phù hợp khi HIS muốn mở BiliTool.Vn trong tab/cửa sổ mới, tự điền dữ liệu cơ bản, để bác sĩ kiểm tra lại trước khi tính. Deep Link không yêu cầu API key vì không gọi endpoint JSON.</p>
        <div class="his-endpoint" tabindex="0"><code>GET</code><span>https://bilitool.vn/may-tinh?ngaysinh=15/03/2026&amp;giosinh=08:30&amp;ngaymau=16/03/2026&amp;giomau=10:00&amp;bili=12.5&amp;donvi=MgDl&amp;tuoithai=38&amp;autocalc=true</span></div>

        <div class="table-responsive" tabindex="0">
            <table class="his-table">
                <thead>
                    <tr><th>Tham số</th><th>Bắt buộc</th><th>Định dạng hợp lệ</th><th>Ghi chú</th></tr>
                </thead>
                <tbody>
                    <tr><td><code>ngaysinh</code></td><td>Có nếu auto tính</td><td><code>dd/MM/yyyy</code>, <code>d/M/yyyy</code>, hoặc <code>ddMMyyyy</code></td><td>Ngày sinh.</td></tr>
                    <tr><td><code>giosinh</code></td><td>Khuyến nghị</td><td><code>HH:mm</code> hoặc <code>HHmm</code></td><td>Nếu bỏ trống, UI dùng giờ mặc định.</td></tr>
                    <tr><td><code>ngaymau</code></td><td>Có nếu auto tính</td><td><code>dd/MM/yyyy</code>, <code>d/M/yyyy</code>, hoặc <code>ddMMyyyy</code></td><td>Ngày lấy mẫu bilirubin.</td></tr>
                    <tr><td><code>giomau</code></td><td>Khuyến nghị</td><td><code>HH:mm</code> hoặc <code>HHmm</code></td><td>Nếu bỏ trống, UI dùng giờ mặc định.</td></tr>
                    <tr><td><code>bili</code></td><td>Có nếu auto tính</td><td>Số thập phân dùng dấu chấm, ví dụ <code>12.5</code></td><td>Bilirubin toàn phần.</td></tr>
                    <tr><td><code>donvi</code></td><td>Không</td><td><code>MgDl</code> hoặc <code>UmolL</code></td><td>Mặc định giao diện là <code>UmolL</code>.</td></tr>
                    <tr><td><code>tuoithai</code></td><td>Có nếu auto tính</td><td>Số nguyên <code>35–45</code></td><td>Tuổi thai khi sinh theo tuần.</td></tr>
                    <tr><td><code>autocalc</code></td><td>Không</td><td><code>true</code> hoặc <code>false</code></td><td><code>true</code> tự chạy tính toán sau khi mở trang.</td></tr>
                </tbody>
            </table>
        </div>

        <div class="his-note warning"><i class="fas fa-circle-info"></i><span>Deep Link hiện chưa hỗ trợ truyền yếu tố nguy cơ, ETCOc hoặc trạng thái chiếu đèn. Dữ liệu này cần bác sĩ chọn trên giao diện hoặc HIS gọi REST API.</span></div>

        <h3>Ví dụ JavaScript trong HIS</h3>
        <pre tabindex="0"><code>// Mở BiliTool.Vn từ EMR bằng Deep Link
const url = new URL("https://bilitool.vn/may-tinh");
url.searchParams.set("ngaysinh", "15/03/2026");
url.searchParams.set("giosinh", "08:30");
url.searchParams.set("ngaymau", "16/03/2026");
url.searchParams.set("giomau", "10:00");
url.searchParams.set("bili", "12.5");
url.searchParams.set("donvi", "MgDl");
url.searchParams.set("tuoithai", "38");
url.searchParams.set("autocalc", "true");

window.open(url.toString(), "_blank", "width=1200,height=800");</code></pre>
    </section>

    <section class="his-method-card">
        <div class="his-method-heading">
            <span>Phương pháp 2</span>
            <h2>REST API cho HIS/EMR</h2>
        </div>
        <p>Phù hợp khi HIS cần nhận kết quả JSON để lưu vào bệnh án điện tử, hiển thị trong màn hình nội bộ hoặc tích hợp workflow tự động. API yêu cầu header <code>X-API-Key</code> và đang áp dụng rate limit <code>30 request/phút</code> cho mỗi cửa sổ giới hạn.</p>
        <div class="his-endpoint" tabindex="0"><code>POST</code><span>https://bilitool.vn/api/v1/bilirubin/calculate</span></div>

        <div class="his-note"><i class="fas fa-lock"></i><span>Production chỉ cấp API key cho đơn vị tích hợp. Nếu chưa cấu hình khóa, API trả <code>503</code>; nếu thiếu hoặc sai khóa, API trả <code>401</code>.</span></div>

        <h3>Header bắt buộc</h3>
        <pre tabindex="0"><code>Content-Type: application/json
X-API-Key: {integration_api_key}</code></pre>

        <h3>Trường đầu vào chính</h3>
        <div class="table-responsive" tabindex="0">
            <table class="his-table">
                <thead>
                    <tr><th>Trường JSON</th><th>Bắt buộc</th><th>Kiểu</th><th>Giới hạn / ý nghĩa</th></tr>
                </thead>
                <tbody>
                    <tr><td><code>tuoiTheoGio</code></td><td>Có nếu không gửi ngày giờ</td><td>number</td><td>Tuổi lúc lấy mẫu, hợp lệ <code>1–336</code> giờ.</td></tr>
                    <tr><td><code>ngaySinh</code></td><td>Có nếu không gửi <code>tuoiTheoGio</code></td><td>ISO datetime</td><td>Có thể gửi kèm giờ, ví dụ <code>2026-03-15T08:30:00+07:00</code>.</td></tr>
                    <tr><td><code>gioSinh</code></td><td>Không</td><td><code>HH:mm:ss</code></td><td>Nếu gửi riêng, hệ thống dùng ngày của <code>ngaySinh</code> cộng giờ này.</td></tr>
                    <tr><td><code>ngayLayMau</code></td><td>Có nếu không gửi <code>tuoiTheoGio</code></td><td>ISO datetime</td><td>Phải sau thời điểm sinh, tuổi tính được <code>1–336</code> giờ.</td></tr>
                    <tr><td><code>gioLayMau</code></td><td>Không</td><td><code>HH:mm:ss</code></td><td>Nếu gửi riêng, hệ thống dùng ngày của <code>ngayLayMau</code> cộng giờ này.</td></tr>
                    <tr><td><code>tongBilirubin</code></td><td>Có</td><td>number</td><td>&gt;0; tối đa <code>60</code> nếu mg/dL, <code>1026</code> nếu µmol/L.</td></tr>
                    <tr><td><code>donViDo</code></td><td>Không</td><td>number enum</td><td><code>0</code> = MgDl, <code>1</code> = UmolL. Mặc định <code>1</code>.</td></tr>
                    <tr><td><code>tuoiThaiTuan</code></td><td>Có</td><td>integer</td><td>Hợp lệ <code>35–45</code> tuần.</td></tr>
                    <tr><td><code>yeuToNguyCo</code></td><td>Không</td><td>object</td><td>Không gửi thì mặc định tất cả yếu tố là <code>false/null</code>.</td></tr>
                    <tr><td><code>trangThaiChieuDen</code></td><td>Không</td><td>number enum</td><td><code>0</code> không chiếu đèn, <code>1</code> đang chiếu, <code>2</code> chiếu đèn tích cực, <code>3</code> đã dừng.</td></tr>
                </tbody>
            </table>
        </div>

        <h3>Yếu tố nguy cơ hỗ trợ trong API</h3>
        <div class="his-risk-grid">
            <div>
                <h4>AAP 2022 - nguy cơ độc thần kinh</h4>
                <code>benhTanHuyetMienDichHoacThieuG6PD</code>
                <code>nhiemKhuanHuyetHoacNghiNgo</code>
                <code>albuminThapDuoi3gDl</code>
                <code>tinhTrangLamSangKhongOnDinh</code>
                <code>etCOcPpm</code>
            </div>
            <div>
                <h4>NICE CG98 - nguy cơ lâm sàng / mở rộng</h4>
                <code>anhChiBiVangDaCanChieuDen</code>
                <code>meBuMeHoanToan</code>
                <code>vangDaTrong24hDau</code>
                <code>benhTanHuyetRh</code>
                <code>benhTanHuyetABO</code>
                <code>dauHieuBenhNaoBilirubinCap</code>
            </div>
        </div>

        <h3>Request mẫu đúng theo DTO hiện tại</h3>
        <pre tabindex="0"><code>{
  "tuoiTheoGio": null,
  "ngaySinh": "2026-03-15T08:30:00+07:00",
  "ngayLayMau": "2026-03-16T10:00:00+07:00",
  "tongBilirubin": 12.5,
  "donViDo": 0,
  "tuoiThaiTuan": 38,
  "trangThaiChieuDen": 0,
  "yeuToNguyCo": {
    "benhTanHuyetMienDichHoacThieuG6PD": false,
    "nhiemKhuanHuyetHoacNghiNgo": false,
    "albuminThapDuoi3gDl": false,
    "tinhTrangLamSangKhongOnDinh": false,
    "etCOcPpm": null,
    "anhChiBiVangDaCanChieuDen": false,
    "meBuMeHoanToan": false,
    "vangDaTrong24hDau": false,
    "benhTanHuyetRh": false,
    "benhTanHuyetABO": false,
    "dauHieuBenhNaoBilirubinCap": false
  }
}</code></pre>

        <h3>Nhóm trường kết quả quan trọng</h3>
        <div class="table-responsive" tabindex="0">
            <table class="his-table">
                <thead><tr><th>Trường trả về</th><th>Ý nghĩa</th></tr></thead>
                <tbody>
                    <tr><td><code>tuoiGio</code>, <code>tuoiThaiTuan</code></td><td>Ngữ cảnh bệnh nhi sau khi hệ thống tính/chuẩn hóa.</td></tr>
                    <tr><td><code>bilirubinMgDl</code>, <code>bilirubinUmolL</code></td><td>Giá trị bilirubin quy đổi song song 2 đơn vị.</td></tr>
                    <tr><td><code>nguongChieuDen</code>, <code>nguongChieuDenTichCuc</code>, <code>nguongThayCuuMau</code></td><td>Ngưỡng AAP 2022 theo mg/dL.</td></tr>
                    <tr><td><code>nguongChieuDen_NICE_UmolL</code>, <code>nguongThayCuuMau_NICE_UmolL</code></td><td>Ngưỡng NICE CG98 theo µmol/L.</td></tr>
                    <tr><td><code>canChieuDenNgay</code>, <code>canChieuDenTichCuc</code>, <code>canXemXetThayCuuMau</code></td><td>Cờ quyết định để HIS hiển thị cảnh báo.</td></tr>
                    <tr><td><code>gioDoLapTiepTheo</code>, <code>lichDoLapNICE</code></td><td>Gợi ý thời điểm đo lại khi phác đồ trả về.</td></tr>
                    <tr><td><code>phacDoQuyetDinh</code>, <code>chuThichThamChieu</code></td><td>Phác đồ quyết định và ghi chú nguồn tham chiếu.</td></tr>
                    <tr><td><code>chartData</code></td><td>Dữ liệu nomogram để HIS tự vẽ biểu đồ nếu cần.</td></tr>
                </tbody>
            </table>
        </div>
    </section>

    <section class="his-method-card">
        <div class="his-method-heading">
            <span>Tham khảo nâng cao</span>
            <h2>API v2 clinical wrapper</h2>
        </div>
        <p>Endpoint <code>POST /api/v2/clinical/bilirubin/calculate</code> dùng cùng input như API v1 nhưng bọc kết quả theo cấu trúc clinical wrapper: <code>guideline</code>, <code>patientContext</code>, <code>thresholds</code>, <code>recommendation</code>, <code>legacyResult</code>. Endpoint này cũng yêu cầu <code>X-API-Key</code>.</p>
        <div class="his-endpoint" tabindex="0"><code>GET</code><span>https://bilitool.vn/api/v2/clinical/bilirubin/guidelines/active</span></div>
    </section>

    <section class="his-method-card his-error-card">
        <div class="his-method-heading">
            <span>Vận hành</span>
            <h2>Mã lỗi cần HIS xử lý</h2>
        </div>
        <div class="his-error-grid">
            <div><strong>400</strong><span>Dữ liệu không hợp lệ: thiếu tuổi/ngày mẫu, tuổi ngoài 1–336 giờ, tuổi thai ngoài 35–45 tuần, bilirubin vượt giới hạn.</span></div>
            <div><strong>401</strong><span>Thiếu hoặc sai <code>X-API-Key</code>.</span></div>
            <div><strong>429</strong><span>Vượt rate limit API.</span></div>
            <div><strong>503</strong><span>Máy chủ chưa cấu hình danh sách API key tích hợp.</span></div>
            <div><strong>500</strong><span>Lỗi hệ thống; response có <code>traceId</code> để đối soát log.</span></div>
        </div>
    </section>
</div>


}

<style>
    .his-doc-shell {
        --his-ink: #07111f;
        --his-muted: #526070;
        --his-blue: #0f5f8f;
        --his-teal: #0f766e;
        --his-line: rgba(15, 95, 143, 0.16);
        max-width: 1180px;
        width: 100%;
        min-width: 0;
        margin: 0 auto;
        padding: clamp(18px, 3vw, 34px);
        color: var(--his-ink);
        font-family: "Inter", "Segoe UI", system-ui, -apple-system, sans-serif;
    }

    .his-hero-panel,
    .his-audit-card,
    .his-method-card {
        border: 1px solid rgba(255,255,255,0.72);
        background: linear-gradient(145deg, rgba(255,255,255,0.96), rgba(247,252,255,0.86));
        box-shadow: 0 24px 70px rgba(8, 34, 55, 0.11), inset 0 1px 0 rgba(255,255,255,0.9);
        border-radius: 30px;
        padding: clamp(24px, 4vw, 44px);
        margin-bottom: 22px;
        overflow: hidden;
        min-width: 0;
    }

    .his-hero-panel {
        position: relative;
        background:
            radial-gradient(circle at 14% 16%, rgba(15,118,110,0.16), transparent 28%),
            radial-gradient(circle at 88% 10%, rgba(15,95,143,0.20), transparent 30%),
            linear-gradient(135deg, rgba(255,255,255,0.98), rgba(234,248,255,0.88));
    }

    .his-kicker,
    .his-method-heading span {
        display: inline-flex;
        align-items: center;
        gap: 9px;
        color: var(--his-blue);
        font-size: 0.78rem;
        font-weight: 900;
        letter-spacing: 0.08em;
        text-transform: uppercase;
    }

    .his-hero-panel h1,
    .his-method-heading h2 {
        margin: 14px 0;
        color: var(--his-ink);
        font-weight: 900;
        letter-spacing: -0.045em;
        line-height: 1.05;
    }

    .his-hero-panel h1 {
        max-width: 820px;
        font-size: clamp(2.1rem, 5vw, 4.4rem);
        outline: none !important;
        border: 0 !important;
    }

    .his-hero-panel p,
    .his-method-card p,
    .his-audit-card li,
    .his-note {
        color: var(--his-muted);
        line-height: 1.72;
        font-weight: 560;
    }

    .his-hero-panel code,
    .his-note code,
    .his-table code,
    .his-check-list code,
    .his-method-card p code {
        color: #0b5f8a;
        background: rgba(14, 165, 233, 0.08);
        border: 1px solid rgba(14, 165, 233, 0.13);
        border-radius: 7px;
        padding: 2px 6px;
    }

    .his-status-grid,
    .his-error-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 12px;
        margin-top: 26px;
    }

    .his-status-grid div,
    .his-error-grid div {
        border: 1px solid var(--his-line);
        border-radius: 20px;
        padding: 16px;
        background: rgba(255,255,255,0.72);
    }

    .his-status-grid strong,
    .his-error-grid strong {
        display: block;
        color: var(--his-blue);
        font-size: 1rem;
        font-weight: 900;
    }

    .his-status-grid span,
    .his-error-grid span {
        display: block;
        color: var(--his-muted);
        margin-top: 7px;
        line-height: 1.45;
        font-weight: 650;
    }

    .his-audit-card h2 {
        color: var(--his-ink);
        font-size: 1.45rem;
        font-weight: 900;
        margin-bottom: 18px;
    }

    .his-check-list {
        list-style: none;
        padding: 0;
        margin: 0;
        display: grid;
        gap: 12px;
    }

    .his-check-list li {
        display: grid;
        grid-template-columns: 30px 1fr;
        gap: 12px;
        align-items: start;
    }

    .his-check-list i {
        width: 30px;
        height: 30px;
        display: inline-grid;
        place-items: center;
        border-radius: 50%;
        color: #0f766e;
        background: rgba(15,118,110,0.12);
    }

    .his-check-list .fa-triangle-exclamation {
        color: #b45309;
        background: rgba(245,158,11,0.14);
    }

    .his-method-heading {
        margin-bottom: 16px;
    }

    .his-method-heading h2 {
        font-size: clamp(1.55rem, 3vw, 2.3rem);
    }

    .his-endpoint {
        display: flex;
        gap: 14px;
        align-items: center;
        margin: 22px 0;
        padding: 16px;
        border: 1px solid var(--his-line);
        border-radius: 18px;
        background: #f8fafc;
        overflow-x: auto;
        max-width: 100%;
        overscroll-behavior-inline: contain;
    }

    .his-endpoint > code {
        flex: 0 0 auto;
        color: #fff;
        background: linear-gradient(135deg, #0f5f8f, #0f766e);
        border: 0;
        border-radius: 10px;
        padding: 8px 12px;
        font-weight: 900;
    }

    .his-endpoint span {
        color: #0f172a;
        font-family: "JetBrains Mono", "SFMono-Regular", Consolas, monospace;
        font-weight: 800;
        white-space: nowrap;
    }

    .his-table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0;
        overflow: hidden;
        border: 1px solid rgba(15,23,42,0.08);
        border-radius: 18px;
        background: #fff;
        margin: 18px 0;
        min-width: 760px;
    }

    .his-table th,
    .his-table td {
        padding: 14px 16px;
        border-bottom: 1px solid rgba(15,23,42,0.07);
        vertical-align: top;
        color: var(--his-muted);
        line-height: 1.5;
    }

    .his-table th {
        color: var(--his-ink);
        background: #f5f9fc;
        font-weight: 900;
    }

    .his-table tr:last-child td {
        border-bottom: 0;
    }

    .his-note {
        display: grid;
        grid-template-columns: 30px 1fr;
        gap: 12px;
        align-items: start;
        margin: 18px 0;
        padding: 16px;
        border: 1px solid rgba(15,118,110,0.16);
        border-radius: 18px;
        background: rgba(240,253,250,0.72);
    }

    .his-note.warning {
        border-color: rgba(245,158,11,0.22);
        background: rgba(255,251,235,0.78);
    }

    .his-note i {
        color: var(--his-teal);
        font-size: 1.05rem;
        margin-top: 4px;
    }

    .his-method-card h3 {
        margin: 28px 0 12px;
        color: var(--his-ink);
        font-size: 1.2rem;
        font-weight: 900;
    }

    .his-risk-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 14px;
    }

    .his-risk-grid div {
        border: 1px solid var(--his-line);
        border-radius: 20px;
        padding: 18px;
        background: rgba(255,255,255,0.72);
    }

    .his-risk-grid h4 {
        color: var(--his-ink);
        font-weight: 900;
        font-size: 1rem;
        margin-bottom: 12px;
    }

    .his-risk-grid code {
        display: inline-block;
        margin: 4px 4px 4px 0;
        color: #0b5f8a;
        background: rgba(14,165,233,0.08);
        border: 1px solid rgba(14,165,233,0.13);
        border-radius: 999px;
        padding: 5px 9px;
        font-size: 0.82rem;
    }

    pre {
        border: 1px solid rgba(15,23,42,0.12);
        border-radius: 20px;
        background: #07111f;
        color: #d9f99d;
        padding: 18px;
        overflow-x: auto;
        max-width: 100%;
        overscroll-behavior-inline: contain;
        box-shadow: inset 0 1px 0 rgba(255,255,255,0.08);
    }

    pre code {
        color: inherit;
        background: transparent;
        border: 0;
        padding: 0;
        font-family: "JetBrains Mono", "SFMono-Regular", Consolas, monospace;
        font-size: 0.9rem;
        line-height: 1.62;
    }

    @@media (max-width: 900px) {
        .his-status-grid,
        .his-error-grid,
        .his-risk-grid {
            grid-template-columns: 1fr;
        }
        .his-doc-shell { padding: 8px 0 24px; }
    }

    @@media (max-width: 560px) {
        .his-doc-shell {
            padding: 6px 0 20px;
        }

        .his-hero-panel,
        .his-audit-card,
        .his-method-card {
            border-radius: 24px;
            padding: 20px 16px;
        }

        .his-hero-panel h1 { font-size: clamp(2rem, 11vw, 3rem); }
        .his-endpoint { align-items: flex-start; }
        .his-endpoint span { white-space: normal; overflow-wrap: anywhere; }
        pre { border-radius: 16px; padding: 14px; }

        .his-check-list li,
        .his-note {
            grid-template-columns: 1fr;
        }
    }
</style>
